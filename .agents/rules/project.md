---
trigger: always_on
---

# Project Context
This is a Desktop Client Application (C# ).

# Agent Rules
- NEVER open web browsers or attempt web-based screen captures.
- Do NOT run `npm` commands or attempt to serve web pages.
- Focus strictly on generating production-ready C# and XAML code.
- Assume the user will handle all UI rendering, compiling (e.g., via Visual Studio or `dotnet build`), and visual QA testing locally.